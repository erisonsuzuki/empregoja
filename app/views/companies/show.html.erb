<div id="company-body">
  <div class="company-container">
    <h1> <%= @company.name %></h1>
    <p> <%= @company.location %></p>
    <p> <%= @company.mail %></p>
    <p> <%= @company.phone %></p>
  </div>

  <% if @company.jobs.any? %>
  	<%= render 'jobs_by_company', jobs: @company.jobs %>
  <% else %>
  	<div class="message">Nenhuma vaga disponível</div>
  <% end %>

  <%= link_to 'Voltar', root_path %>
</div>